%section.genre
  %header.clearfix
    %h1= @category.name.titleize
    .genre_sorting
      Sort by:
      %select(name="")
        %option(value="a-z") Title: A - Z
        %option(value="z-a") Title: Z - A
        %option(value="rating") Rating
  %article.row
    = render @category.videos